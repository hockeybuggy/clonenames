[build]
  publish = "dist"
  functions = "functions"
  command = "yarn run production-build"

[dev]
  # Local dev command. A.k.a npm start
  command = "yarn run start"
  targetPort = 8080
  functionsPort = 34567
  publish = "dist"
  autoLaunch = false
