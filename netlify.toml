[build]
  publish = "build"
  functions = "functions"
  command = "production-build"

[dev]
  # Local dev command. A.k.a npm start
  command = "yarn run start"
  targetPort = 8080
  functionsPort = 34567
  publish = "build"
  autolaunch = false
